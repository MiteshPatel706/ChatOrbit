import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ImageDetailHeader = () => {
  return (
    <View>
      <Text>ImageDetailHeader</Text>
    </View>
  );
};

export default ImageDetailHeader;

const styles = StyleSheet.create({});
